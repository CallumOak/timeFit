import { FrequencyRoutinePlan } from './frequency-routine-plan.model';

describe('FrequencyRoutinePlan', () => {
  it('should create an instance', () => {
    expect(new FrequencyRoutinePlan()).toBeTruthy();
  });
});
