import { IndividualRoutinePlan } from './individual-routine-plan.model';

describe('IndividualRoutinePlan', () => {
  it('should create an instance', () => {
    expect(new IndividualRoutinePlan()).toBeTruthy();
  });
});
