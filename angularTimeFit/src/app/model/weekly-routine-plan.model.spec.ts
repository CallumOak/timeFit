import { WeeklyRoutinePlan } from './weekly-routine-plan.model';

describe('WeeklyRoutinePlan', () => {
  it('should create an instance', () => {
    expect(new WeeklyRoutinePlan()).toBeTruthy();
  });
});
